// Get Message
function getMessage() {
    return "hello"
     
}

const string = getMessage();

// Add Two Numbers
function addNumbers(a, b) {
    return a + b;
}

addNumbers(1, 2); //  3


// Multiplying Numbers

function product(a,b) {
    return a*b;
}
product(2,4) //8



// Averaging Numbers
function average(a, b, c, d) {
    return average = (a+ b + c+d) / 4;

}
average (1,1,1,1)// 1

//Number Randomness
function getRandom() {
    return Math.random();
    }

    //EX
    // randomNumber will be between 25 and 100
const randomNumber = (Math.random() * 75) + 25; //25-100

   //Floor :- The function will round a number down to the nearest integer. For example if we had the number, 2.9999, the function will round this input down to 2.
   function getFloor(x) {
    return Math.floor(2.13434);//2
    
}



